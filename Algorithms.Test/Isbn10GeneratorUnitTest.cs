using Algorithms.Library;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Algorithms.Test
{
    [TestClass]
    public class Isbn10GeneratorUnitTest
    {
        [TestMethod]
        public void Isbn10TestVer0()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer1()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer2()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer3()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer4()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer5()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer6()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer7()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer8()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer9()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer10()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer11()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer12()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer13()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer14()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer15()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer16()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer17()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer18()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer19()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer20()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer21()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer22()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer23()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer24()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer25()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer26()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer27()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer28()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }

        [TestMethod]
        public void Isbn10TestVer29()
        {
            string isbn = Common.Isbn10.Generate();
            Assert.AreEqual(true, Common.Isbn10.ValidateIsbn(isbn));
        }
    }
}